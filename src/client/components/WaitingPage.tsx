/** Displays a message while the user is waiting for all players to join */
export function WaitingPage() {
  return (
    <div className="WaitingPage">
      <h2>Waiting for all players to join...</h2>
    </div>
  );
}
